<!DOCTYPE html>
    <%- include('../components/head')%> <!--tag do EJS que executa o comando de incluir um arquivo, no cado o arquivo do head.ejs-->
    <body class="container">
        <%- include('../components/header')%> <!--<%- include('../components/header', {pagina: 'Home'})%>, passaria o objeto Home para o header mas decidimos usar um foreach no header para caso não seja recebido objeto algum, ele exiba o Home-->
        <main>
            <div class="jumbotron">
                <h1>Node com EJS</h1>
                <p>Aprendendo a trabalhar com arquivos EJS</p>
                <p><%= subtitle %></p>
            </div>
            <ul>
                <% qualitys.forEach(function(quality){ %> <!--Fechou aqui a aba de ESJ mas como o que está entre essa função aberta e as chaves fechando embaixo é o código html, ele executa o html e depois fecha as chaves em EJS-->
                    <li>
                        <strong><%= quality.title %></strong><%= quality.message %>
                    </li>
                <% }) %> <!--fechando as chaves da function acima-->
            </ul>
        </main>
        <%- include('../components/footer')%>
    </body>
</html>